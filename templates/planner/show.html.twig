{% extends 'base.html.twig' %}

{% block title %}Planner{% endblock %}

{% block body %}
    <h1>Planner</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ planner.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_planner') }}">back to list</a>

    <a href="{{ path('app_planner_edit', {'id': planner.id}) }}">edit</a>

    {{ include('planner/_delete_form.html.twig') }}
{% endblock %}
