<form method="post" action="{{ path('app_ingredient_list_delete', {'id': ingredient_list.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ingredient_list.id) }}">
    <button class="btn">Delete</button>
</form>
